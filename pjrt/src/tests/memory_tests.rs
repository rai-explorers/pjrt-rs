//! Unit Tests for Memory Module
//!
//! The `Memory` struct requires a valid PJRT plugin for all operations.
//! Unit tests without a plugin are not possible — see integration tests
//! in the `examples/` directory instead.
